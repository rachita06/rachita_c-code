#include<stdio.h>
struct student
{ 
char name[20];
char add[20];
int id};


	int main()
{

struct student s;
{

printf("enter name,add,&id");
scanf("%s%s%d",s.name,s.add,s.id);}
struct student;
{
printf("the name is %s,add is %s,id is %d",s.name,s.add,&s.id);
return 0;
}
