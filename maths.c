#include<stdio.h>
#include<math.h>
int main()
{
	int a,b,c;
	float S,A;
	printf("enter three sides of scalene triangle");
	scanf("%d%d%d",&a,&b,&c);
	S=(a*b*c)/3
	A=sqrt (s*(s-a)*(s-b)*(s-c))
	printf("Area is%f",A);
	return 0;
	
}
